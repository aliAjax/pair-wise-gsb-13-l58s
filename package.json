{
  "name": "gas-shift-handover",
  "private": true,
  "version": "1.0.0",
  "description": "加油站班次交接：班中调价按价格段拆账、泵码接续校验、长短款原因复核、冻结凭证订正留痕。零依赖、零构建。",
  "scripts": {
    "start": "node server.js",
    "test": "node --check js/rules.js && node --check js/storage.js && node --check js/app.js && node test/rules.test.js"
  }
}
